# Approval request observation fixtures

These sanitized fixtures freeze the Phase 3.5.3a observation boundary for
three upstream approval request kinds, `serverRequest/resolved`, exact
`item/completed` correlation, and upstream auto-review lifecycle annotation.

They contain no command text, filesystem paths, authentication material,
decision result, client identity, owner, or lease information.
