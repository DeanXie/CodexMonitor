# Projection freshness fixtures

These sanitized fixtures freeze the Phase 3.5.4a read-only snapshot schema.
Phase 3.5.4e reuses them through
`../phase-3-5-4-compatibility/fixture-family-manifest.json`; it does not create a
second freshness authority. Values are synthetic and contain no credentials,
local paths, client identity, ownership, lease, or inferred Thread-existence
claims.
