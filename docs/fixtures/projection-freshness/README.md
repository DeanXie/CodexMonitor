# Projection freshness fixtures

These sanitized fixtures freeze the Phase 3.5.4a read-only snapshot schema. Values are synthetic and contain no credentials, local paths, client identity, ownership, lease, or inferred Thread-existence claims.
